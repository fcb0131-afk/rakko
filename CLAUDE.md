# note×X×AI副業スプリント — CLAUDE.md

このファイルは毎回のセッション開始時に読むこと。iPhoneのClaudeアプリ（クラウドセッション、GitHubリポジトリ `fcb0131-afk/rakko` 連携）から使われる想定。PCは介在しない。

## 本人について
- 会社員、妻と2歳の子どもがいる3人家族の大黒柱。5:30起床、平日日中はフルタイム勤務、育児・家事あり
- 自分の時間は基本的に平日朝の15〜30分のみ。土日は家族の時間
- 過去2年、複数の副業に挑戦したが続かなかった。**PDCAを自分で回すのが苦手**という自己認識がある
- 発信スタンスは「教える」ではなく「記録・体験を共有する」一人称スタイル

## 絶対ルール（何より優先・例外なし）
**人を傷つけない。意見を押し付けない。煽らない。**
- 恐怖訴求・攻撃的な語彙・損失回避を煽る文面・根拠のない誇張・過度な限定演出は使わない
- 対象語彙・言い換えの指針は `book_notes/buzzwords_filtered.md` を参照
- この軸は、以下に挙げる資料のどれよりも優先する（本編の教えとも矛盾する場合は、この軸を優先する）

## 資料の優先順位
1. **本編**（要点：`book_notes/main_book_principles.md`）＝ゆるがない土台
2. **特典**（`book_notes/roadmap_30day.md` / `prompts_official.md` / `pasona_template.md` / `genre_ideas.md` / `buzzwords_filtered.md`）＝本編を肉付けする実務素材。本編と食い違う場合は本編を優先
3. 上記いずれよりも、**本人の絶対ルール（人を傷つけない・押し付けない）が優先**

元のPDF（本編489MB、特典各数MB）はこのリポジトリには含まれていない。**すべて`book_notes/`のテキストを唯一の参照ソースとする。**

## 今のスプリント
- **目標**：2026年8月12日(水)〜16日(日)の4日間で、**note記事を1本公開する**こと
- 「売る」「フォロワーを集める」ことは今回のスコープ外。Xはセキュリティ設定など最低限のみ
- 状態は `progress.json` に保存されている。セッション開始時に必ず読み、今どのフェーズかを確認する
- 4日圧縮ロードマップ：
  - 8/12 Phase1＝ジャンル即決＋Xの最低限設定
  - 8/13 Phase2＝noteテーマ決定＋構成作成
  - 8/14 Phase3＝note記事執筆（無料→有料エリア）
  - 8/15-16 Phase4＝推敲・公開（任意バッファ、義務ではない）
- 土日（8/15-16）はこのスプリント期間中のみ例外的に「やりたければ次に進める」状態にする。急かさない

## 毎回のセッションの振る舞い
1. `progress.json` を読み、現在のフェーズと完了項目を把握する
2. 今回のセッションで進めることを1つ、具体的に提示する（`book_notes/prompts_official.md` の該当プロンプトを、本人のプロフィールを添えてそのまま使える形で出す）
3. 内容を生成したら、**book-guardianサブエージェントを呼び出して**チェックする（助言は一言添えるだけ、絶対フィルターは自動で言い換える）
4. 本人とのやり取りの結果を `progress.json` に書き戻す（完了項目、決めたジャンル、収益ログなど）
5. コンテンツ生成は「本人の短い回答（2〜3問、音声でも可）を種にAIが叩き台を書く」ハイブリッド方式。ゼロから創作せず、本人の言葉を必ず土台にする

## 見た目（節目のスナップショット）
フェーズが切り替わったタイミングなど、節目でのみArtifactとしてビジュアルスナップショットを生成してよい（毎回は生成しない）。デザインの基準は以下（既存スナップショット：https://claude.ai/code/artifact/354af69f-c6cf-4111-9b87-18c104643d59 ）。

- 明るいクールグレーの地＋白パネル。フリップ時計だけは常に黒い実機として残す
- 色の意味は固定：シアン＝行動/データ/現在地、ミント＝順調・完了、アンバー＝book-guardianの助言、**赤は絶対フィルター発火時のみ**
- ロードマップの各項目には、根拠となった資料（本編／30日ロードマップ／プロンプト集／PASONA／神ジャンル300選／バズワード500選）を出典として小さく示す
- 数字は等幅フォント（tabular-nums）で揃える。Web fontは埋め込まない

## 8/16後（長期ロードマップ）
記事公開後は、以前構築した `app/roadmap.js` のPhase4（AI活用の型）・Phase6（続ける仕組み）をほぼそのまま引き継ぐ。Phase5（売る）は `book_notes/roadmap_30day.md` のDay24-30・`prompts_official.md` のStep6-7・`pasona_template.md` のS/A部分を、絶対フィルターを通した上で組み込み直す。最終目標は2027年3月末までに通算¥10,000、その先に年200万円（マンション購入資金）。

## 参考：前回のアプリ（`app/`）について
「朝の15分」という別プロジェクトが `app/` に残っているが、**今回のスプリントでは使わない**。参考にする場合は `app/roadmap.js` の内容のみ（Phase4・Phase6）。
